apiVersion: batch/v1
kind: CronJob
metadata:
  name: hello-world
spec: 
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: hello-world
            image: hello-world
            imagePullPolicy: IfNotPresent
            command: ["python3"]
            args: ["hello-world.py"]
          restartPolicy: OnFailure
